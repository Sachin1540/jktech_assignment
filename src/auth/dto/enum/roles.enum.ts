/**
 * Enum representing different roles available in the application.
 *
 * This enum can be used to assign and check user roles throughout the system,
 * including in authentication, authorization, and access control.
 */
export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
  VIEWER = 'VIEWER',
}
